import React from 'react';
import { AppShell } from '../index';

/**
 * WOODFARM CARE 간편 견적 AppShell 사용 예시
 * 
 * 이 컴포넌트는 AppShell을 어떻게 사용하는지 보여주는 예시입니다.
 * 실제 프로젝트에서는 이 컴포넌트를 루트 컴포넌트로 사용하거나
 * 기존 애플리케이션에 통합하여 사용할 수 있습니다.
 */
export function AppShellExample() {
  return (
    <div>
      <AppShell />
    </div>
  );
}

export default AppShellExample;