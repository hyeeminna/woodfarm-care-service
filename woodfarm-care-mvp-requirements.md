# OFFICE CARE 간편 견적 MVP 요구사항 정의서

- 문서 버전: v1.0
- 작성일: 2026-09-16
- 구현 권장 환경: React + TypeScript + Vite
- 서비스 유형: 사무·상업 공간 가구 클리닝·점검·간단 수리·정기관리 간편 견적 웹 MVP

---

## 1. 프로젝트 개요

### 1.1 서비스명

**OFFICE CARE 간편 견적**

### 1.2 핵심 문구

> **사무·상업가구, 교체하기 전에 관리하세요.**  
> 기업용 사무가구부터 카페·병원·식당 등의 가구까지, 클리닝·유지관리 서비스를 간편하게 확인해보세요.

### 1.3 목적

사용자가 회사 또는 상업 공간에서 관리가 필요한 가구와 서비스를 간단한 질문으로 선택하고, 사진을 첨부해 예상 견적 범위를 확인한 뒤 무료 방문견적을 신청할 수 있도록 한다.

### 1.4 핵심 사용자

- 사무실 가구를 교체하기 전 세척·수리 가능 여부를 확인하려는 기업 담당자
- 카페·음식점·병원·학원 등 상업 공간의 가구를 관리하려는 사업자
- 여러 가구를 한 번에 정기적으로 관리하려는 공간 운영자

### 1.5 MVP의 핵심 가치

1. 전화 문의 전에 예상 비용을 빠르게 확인할 수 있다.
2. 사진과 선택 정보를 바탕으로 방문 상담에 필요한 내용을 미리 전달할 수 있다.
3. 가구를 바로 폐기하거나 교체하지 않고 세척·점검·수리 가능성을 먼저 확인할 수 있다.

---

## 2. 사용자 흐름

```text
첫 화면
→ 견적 시작
→ STEP 1 규모 선택
→ STEP 2 가구 종류 및 수량 입력
→ STEP 3 서비스 선택
→ STEP 4 사진 업로드
→ 입력 내용 분석
→ 예상 견적 결과
→ 무료 방문견적 신청
→ 신청 완료
```

### 2.1 공통 흐름 원칙

- 첫 화면을 제외한 질문 화면은 **한 화면에 한 단계**만 보여준다.
- 상단에 `1 / 4` 형태의 단계 표시와 진행 바를 제공한다.
- 각 단계에서 `이전`과 `다음` 버튼을 제공한다.
- 사용자가 이전 단계로 이동해 답변을 수정해도 다른 입력값은 유지한다.
- 질문 단계 어디서든 `처음으로` 버튼으로 초기 화면에 돌아갈 수 있다.
- 결과 화면에서 `조건 다시 선택하기`를 누르면 기존 입력값을 유지한 채 STEP 1로 이동한다.
- 결과 화면에서 `새 견적 시작하기`를 누르면 모든 입력값을 초기화한다.

---

## 3. 화면별 상세 요구사항

## 3.1 화면 1 — 랜딩 화면

### 표시 요소

- 서비스명 또는 로고: `OFFICE CARE`
- 메인 카피: `사무·상업가구, 교체하기 전에 관리하세요.`
- 서브 카피: `기업용 사무가구부터 카페·병원·식당 등의 가구까지, 클리닝·유지관리 서비스를 간편하게 확인해보세요.`
- 주요 CTA: `간편 견적 시작하기`
- 보조 안내: `약 1분 소요 · 예상 견적 확인 · 무료 방문견적 신청`

### 동작

- `간편 견적 시작하기` 클릭 시 STEP 1로 이동한다.

---

## 3.2 화면 2 — STEP 1 규모 선택

### 질문

`공간의 상주 인원은 몇 명인가요?`

### 선택 항목

- `10인 이하`
- `11~30인`
- `31~50인`
- `50인 이상`

### 선택 방식

- 단일 선택만 가능하다.
- 카드 전체를 클릭할 수 있어야 한다.
- 선택된 카드는 테두리, 배경색, 체크 아이콘으로 구분한다.
- 선택 전에는 `다음` 버튼을 비활성화한다.

### 저장 데이터

```ts
type CompanySize = 'UNDER_10' | 'BETWEEN_11_30' | 'BETWEEN_31_50' | 'OVER_50';
```

### 문구 원칙

- 사무실 외 상업 공간도 사용할 수 있도록 화면 질문은 `회사 규모`보다 `공간의 상주 인원`을 우선 사용한다.
- 내부 데이터명은 개발 편의를 위해 `companySize`를 사용할 수 있다.

---

## 3.3 화면 3 — STEP 2 가구 종류 및 수량

### 질문

`관리받고 싶은 가구를 선택해주세요.`

### 선택 항목

- `사무용 의자`
- `파티션`
- `소파`
- `책상`

### 선택 방식

- 복수 선택이 가능하다.
- 가구 카드를 선택하면 해당 카드 안에 수량 입력 영역을 표시한다.
- 선택된 가구는 체크 아이콘과 강조 스타일로 구분한다.
- 가구를 선택 해제하면 해당 가구의 수량도 초기화한다.

### 수량 입력

- 입력 방식: 숫자 직접 입력 또는 `- / +` 스테퍼
- 최소값: `1`
- 최대값: `999`
- 숫자가 아니거나 1 미만인 값은 저장하지 않는다.
- 한 종류 이상의 가구가 선택되고, 선택한 모든 가구의 수량이 유효해야 `다음` 버튼이 활성화된다.

### 저장 데이터

```ts
type FurnitureType = 'CHAIR' | 'PARTITION' | 'SOFA' | 'DESK';

interface FurnitureItem {
  type: FurnitureType;
  quantity: number;
}
```

---

## 3.4 화면 4 — STEP 3 필요한 서비스 선택

### 질문

`어떤 관리가 필요한가요?`

### 선택 항목

- `세척` — 오염·얼룩·먼지 등을 가구 재질에 맞게 클리닝
- `상태점검` — 흔들림·마모·파손 여부와 사용 가능 상태 확인
- `간단수리` — 나사 조임, 부품 점검 등 현장에서 가능한 경정비
- `정기관리` — 일정 주기에 맞춘 반복 점검 및 클리닝 상담

### 선택 방식

- 복수 선택이 가능하다.
- 각 카드에 서비스명과 한 줄 설명을 함께 표시한다.
- 한 개 이상의 서비스를 선택해야 `다음` 버튼이 활성화된다.

### 안내 문구

- `간단수리와 정기관리는 가구 상태 및 관리 주기에 따라 방문 확인 후 최종 금액이 달라질 수 있습니다.`

### 저장 데이터

```ts
type ServiceType = 'CLEANING' | 'INSPECTION' | 'MINOR_REPAIR' | 'REGULAR_CARE';
```

---

## 3.5 화면 5 — STEP 4 사진 업로드

### 질문

`가구 상태를 확인할 수 있는 사진을 올려주세요.`

### 안내 문구

`전체 모습과 오염·파손 부위가 잘 보이는 사진을 올리면 더 정확한 상담이 가능합니다.`

### 업로드 요구사항

- 최소 업로드 수: `0장` — MVP에서는 건너뛰기 허용
- 최대 업로드 수: `5장`
- 허용 형식: `JPG`, `JPEG`, `PNG`, `WEBP`
- 파일당 최대 용량: `10MB`
- 여러 파일을 한 번에 선택할 수 있다.
- 업로드한 사진의 미리보기를 표시한다.
- 각 사진별 삭제 기능을 제공한다.
- 허용되지 않는 형식, 용량 초과, 최대 개수 초과 시 원인을 알 수 있는 오류 메시지를 표시한다.
- 사진을 올리지 않은 경우 `사진 없이 견적 보기` 버튼을 표시한다.

### 개인정보 안내

`사람의 얼굴, 연락처, 문서 등 개인정보가 포함되지 않은 사진을 권장합니다.`

### 저장 데이터

```ts
interface UploadedPhoto {
  id: string;
  file: File;
  previewUrl: string;
}
```

---

## 3.6 화면 6 — 분석 중 화면

### 표시 요소

- 로딩 인디케이터
- 문구: `선택한 조건으로 예상 견적을 계산하고 있어요.`
- 보조 문구: `실제 금액은 현장 상태와 가구 재질에 따라 달라질 수 있습니다.`

### 동작

- 견적 계산은 클라이언트에서 즉시 처리한다.
- 사용자 경험을 위해 로딩 화면은 약 0.8~1.2초 표시할 수 있다.
- 계산 완료 후 예상 견적 결과 화면으로 자동 이동한다.

---

## 3.7 화면 7 — 예상 견적 결과

### 결과 카드 구성

1. 제목: `예상 견적을 확인해보세요.`
2. 선택한 규모
3. 선택한 가구와 각 수량
4. 선택한 서비스
5. 사진 첨부 장수
6. 예상 견적 범위
7. 견적 안내 문구
8. 주요 CTA: `무료 방문견적 신청`
9. 보조 CTA: `조건 다시 선택하기`
10. 보조 CTA: `새 견적 시작하기`

### 예시

> 사무용 의자 30개  
> 파티션 10개  
> 세척 + 상태점검  
> 예상 견적  
> **약 25~32만 원**

### 필수 안내 문구

`위 금액은 입력한 수량과 서비스를 기준으로 계산한 참고용 예상 범위입니다. 가구의 재질, 오염·파손 정도, 작업 환경, 이동 거리 등에 따라 실제 견적은 달라질 수 있습니다.`

### 추가 표시 규칙

- `간단수리`가 포함되면 `수리 비용은 부품과 파손 상태 확인 후 별도로 안내됩니다.`를 표시한다.
- `정기관리`가 포함되면 `방문 주기와 작업 범위를 협의한 뒤 정기관리 견적을 별도로 안내합니다.`를 표시한다.
- 견적 계산이 불가능한 조합이어도 오류 화면으로 보내지 않고 `방문 확인 필요` 결과와 신청 CTA를 표시한다.

---

## 3.8 화면 8 — 무료 방문견적 신청

### 입력 항목

- 담당자명 — 필수
- 업체명 또는 공간명 — 필수
- 연락처 — 필수
- 공간 유형 — 필수
  - `사무실`
  - `카페`
  - `음식점`
  - `병원`
  - `학원`
  - `기타`
- 방문 지역 — 필수
- 문의사항 — 선택
- 개인정보 수집·이용 동의 — 필수

### 자동 포함 데이터

- 규모 선택값
- 가구 종류 및 수량
- 선택 서비스
- 계산된 예상 견적 범위
- 업로드 사진
- 신청 일시

### 유효성 검사

- 담당자명과 업체명은 공백만 입력할 수 없다.
- 연락처는 숫자와 하이픈만 허용한다.
- 필수 항목과 동의가 완료되어야 신청 버튼을 활성화한다.
- 오류가 있는 입력란 바로 아래에 구체적인 안내를 표시한다.

### 신청 완료 화면

- 제목: `방문견적 신청이 완료되었습니다.`
- 설명: `입력한 연락처로 상담 일정을 안내드리겠습니다.`
- 버튼: `처음으로 돌아가기`

---

## 4. 예상 견적 계산 요구사항

## 4.1 계산 목적

- MVP에서는 사용자가 서비스 가격대를 빠르게 이해하도록 **참고용 범위**를 제공한다.
- 웹 화면에 단가를 직접 노출하지 않는다.
- 단가는 코드 한곳의 설정 객체에서 쉽게 수정할 수 있어야 한다.

## 4.2 초기 데모 단가

아래 값은 실제 확정 가격이 아니라 MVP 동작 확인을 위한 초기 설정값이다.

| 가구 | 세척+기본 상태점검 최소 단가 | 세척+기본 상태점검 최대 단가 |
|---|---:|---:|
| 사무용 의자 | 7,000원/개 | 8,500원/개 |
| 파티션 | 4,000원/개 | 6,500원/개 |
| 소파 | 추후 설정 | 추후 설정 |
| 책상 | 추후 설정 | 추후 설정 |

### 기준 예시 검증

```text
사무용 의자 30개 + 파티션 10개 + 세척 + 상태점검
최소: (30 × 7,000) + (10 × 4,000) = 250,000원
최대: (30 × 8,500) + (10 × 6,500) = 320,000원
표시 결과: 약 25~32만 원
```

## 4.3 계산 규칙

1. 선택한 가구별 수량에 최소·최대 단가를 각각 곱한다.
2. 모든 가구의 금액을 합산한다.
3. 결과는 만 원 단위로 표시한다.
4. 세척만 또는 상태점검만 선택한 경우의 계수는 별도 설정값으로 관리한다.
5. `간단수리` 비용은 온라인 예상가에 확정 반영하지 않고 방문 확인 필요 문구를 추가한다.
6. `정기관리` 비용은 온라인 예상가에 확정 반영하지 않고 별도 상담 문구를 추가한다.
7. 단가가 아직 설정되지 않은 가구가 포함되면 금액 대신 `방문 확인 필요`를 표시한다.

### 설정 객체 예시

```ts
const estimateConfig = {
  CHAIR: { min: 7000, max: 8500 },
  PARTITION: { min: 4000, max: 6500 },
  SOFA: null,
  DESK: null,
};
```

---

## 5. 데이터 모델

```ts
interface EstimateFormData {
  companySize: CompanySize | null;
  furniture: FurnitureItem[];
  services: ServiceType[];
  photos: UploadedPhoto[];
}

interface EstimateResult {
  status: 'CALCULATED' | 'VISIT_REQUIRED';
  minPrice: number | null;
  maxPrice: number | null;
  messages: string[];
}

interface VisitRequest {
  managerName: string;
  businessName: string;
  phone: string;
  spaceType: 'OFFICE' | 'CAFE' | 'RESTAURANT' | 'HOSPITAL' | 'ACADEMY' | 'OTHER';
  region: string;
  memo?: string;
  privacyConsent: boolean;
  estimateData: EstimateFormData;
  estimateResult: EstimateResult;
  submittedAt: string;
}
```

---

## 6. 기능 요구사항

| ID | 기능 | 우선순위 | 완료 조건 |
|---|---|---|---|
| FR-01 | 견적 시작 | 필수 | 랜딩 CTA 클릭 시 STEP 1로 이동한다. |
| FR-02 | 단계 진행 표시 | 필수 | STEP 1~4에서 현재 단계와 진행 바를 표시한다. |
| FR-03 | 규모 단일 선택 | 필수 | 한 항목만 선택되고 값이 저장된다. |
| FR-04 | 가구 복수 선택 | 필수 | 여러 가구를 선택·해제할 수 있다. |
| FR-05 | 가구별 수량 입력 | 필수 | 선택한 모든 가구에 유효한 수량을 입력해야 진행된다. |
| FR-06 | 서비스 복수 선택 | 필수 | 한 개 이상의 서비스를 선택할 수 있다. |
| FR-07 | 사진 업로드 | 필수 | 최대 5장의 유효한 이미지를 미리보기·삭제할 수 있다. |
| FR-08 | 사진 없이 진행 | 필수 | 사진을 첨부하지 않아도 결과를 확인할 수 있다. |
| FR-09 | 입력값 유지 | 필수 | 이전 단계 이동 및 결과 수정 시 기존 값이 유지된다. |
| FR-10 | 견적 계산 | 필수 | 설정된 단가와 수량으로 최소·최대 범위를 계산한다. |
| FR-11 | 방문 확인 처리 | 필수 | 미설정 단가 또는 별도 확인 서비스는 방문 확인 필요로 안내한다. |
| FR-12 | 결과 요약 | 필수 | 선택값, 사진 수, 예상 범위, 유의사항을 한 화면에 보여준다. |
| FR-13 | 방문견적 신청 | 필수 | 필수 정보를 검증하고 신청 데이터를 제출한다. |
| FR-14 | 신청 완료 | 필수 | 성공 메시지와 처음으로 돌아가기 버튼을 제공한다. |
| FR-15 | 오류 처리 | 필수 | 업로드·검증·제출 실패 원인과 재시도 방법을 안내한다. |

---

## 7. UI/UX 요구사항

### 7.1 디자인 방향

- 키워드: `Clean`, `Simple`, `Friendly`, `Trustworthy`
- 화이트 또는 밝은 뉴트럴 배경을 기본으로 한다.
- 질문과 결과는 화이트 카드 중심으로 구성한다.
- 포인트 컬러는 한 가지 주조색과 상태 색상만 사용한다.
- 과도한 장식, 게임형 HUD, 복잡한 애니메이션은 사용하지 않는다.
- 사무가구 판매 페이지보다 관리·상담 서비스라는 인상이 먼저 전달되어야 한다.

### 7.2 반응형

- 모바일 우선으로 설계한다.
- 최소 기준 화면 폭: `360px`
- 모바일에서는 선택 카드를 1열 또는 2열로 배치한다.
- 데스크톱에서는 콘텐츠 최대 폭을 약 `720~960px`로 제한한다.
- 주요 CTA는 모바일에서 화면 하단 고정 영역을 사용할 수 있다.

### 7.3 피드백

- 선택 시 즉시 시각적 피드백을 제공한다.
- 버튼 비활성화 상태와 활성화 상태가 명확해야 한다.
- 결과 카드는 위에서 아래로 자연스럽게 나타나는 짧은 애니메이션을 적용할 수 있다.
- 로딩, 성공, 오류 상태를 각각 구분해 표시한다.

### 7.4 접근성

- 모든 입력 요소에 연결된 라벨을 제공한다.
- 키보드만으로 선택, 이동, 제출할 수 있어야 한다.
- 선택 상태를 색상만으로 구분하지 않고 체크 아이콘과 텍스트를 함께 사용한다.
- 버튼과 본문은 충분한 명도 대비를 확보한다.
- 업로드 이미지에는 사용자가 확인 가능한 파일명을 표시한다.

---

## 8. 비기능 요구사항

### 8.1 성능

- 첫 화면은 일반적인 모바일 네트워크에서 빠르게 표시되어야 한다.
- 업로드 이미지 미리보기 생성 시 브라우저가 멈추지 않아야 한다.
- 불필요한 대용량 이미지와 라이브러리 사용을 피한다.

### 8.2 보안 및 개인정보

- 연락처와 사진은 사용자 동의 없이 제3자에게 제공하지 않는다.
- 운영 버전에서는 HTTPS를 사용한다.
- 파일의 확장자뿐 아니라 MIME 타입도 검증한다.
- 업로드 파일명을 그대로 서버 경로로 사용하지 않는다.
- 신청 데이터 저장 또는 외부 전송 기능을 연결할 때 개인정보처리방침 링크를 제공한다.

### 8.3 호환성

- 최신 Chrome, Edge, Safari 모바일·데스크톱 환경을 우선 지원한다.
- 터치와 마우스 입력 모두 지원한다.

---

## 9. MVP 구현 범위

### 포함

- 랜딩 화면
- 4단계 질문형 견적 플로우
- 가구별 수량 입력
- 이미지 선택, 미리보기, 삭제
- 설정 기반 예상 견적 계산
- 결과 요약 및 유의사항
- 방문견적 신청 폼
- 반응형 UI
- 입력값 검증 및 기본 오류 처리

### 이번 MVP에서 제외

- 회원가입 및 로그인
- 온라인 결제
- 작업 일정 자동 예약
- 관리자 페이지
- 실제 AI 이미지 분석
- 사진만으로 오염도·파손도를 자동 판정하는 기능
- 지도 기반 방문 가능 지역 자동 판정
- 고객별 견적 이력 관리
- 정기관리 계약 및 자동 결제
- 실제 문자·카카오톡 자동 발송

---

## 10. 구현 단계 권장안

### 1단계 — 프론트엔드 데모

- 모든 질문과 결과 계산을 브라우저에서 처리한다.
- 사진은 미리보기만 제공하고 서버에 저장하지 않는다.
- 신청 버튼 클릭 시 성공 화면을 보여주되 실제 외부 전송은 하지 않는다.
- 개발 환경에서는 제출 데이터를 콘솔 또는 로컬 상태에서 확인할 수 있다.

### 2단계 — 제출 기능 연결

- 방문견적 신청 데이터를 서버 또는 데이터 저장 서비스에 전송한다.
- 사진 업로드 저장소를 연결한다.
- 제출 성공·실패 응답을 화면에 반영한다.

### 3단계 — 운영 고도화

- 실제 단가표 반영
- 공간 유형, 재질, 오염·파손 상태 질문 추가
- 방문 가능 지역과 출장비 계산
- 신청 알림과 고객 관리 기능 연결

---

## 11. 테스트 및 인수 기준

1. 사용자는 모바일 360px 화면에서도 가로 스크롤 없이 모든 단계를 완료할 수 있다.
2. STEP 1에서 규모를 선택하지 않으면 다음 단계로 이동할 수 없다.
3. STEP 2에서 선택한 가구의 수량이 비어 있거나 1 미만이면 다음 단계로 이동할 수 없다.
4. STEP 3에서 서비스를 선택하지 않으면 다음 단계로 이동할 수 없다.
5. STEP 4에서 사진 없이도 결과를 확인할 수 있다.
6. 10MB 초과 파일 또는 허용되지 않은 형식은 업로드되지 않고 오류가 표시된다.
7. 이전 단계로 돌아가도 기존 선택값과 업로드 사진이 유지된다.
8. `의자 30개 + 파티션 10개 + 세척 + 상태점검` 입력 시 초기 데모 설정 기준 `약 25~32만 원`이 표시된다.
9. 단가가 설정되지 않은 소파 또는 책상이 포함되면 잘못된 금액 대신 `방문 확인 필요`가 표시된다.
10. 결과 화면에는 참고용 금액이며 실제 견적이 달라질 수 있다는 안내가 항상 표시된다.
11. 방문견적 신청의 필수 입력과 개인정보 동의가 없으면 제출할 수 없다.
12. 신청 성공 후 완료 화면이 표시되고 처음 화면으로 돌아갈 수 있다.

---

## 12. 향후 확장 시 추가할 질문

MVP 검증 후 견적 정확도를 높이려면 아래 흐름으로 확장한다.

```text
공간 유형
→ 공간/회사 규모
→ 가구 종류와 수량
→ 오염·파손 상태
→ 필요한 서비스
→ 사진 업로드
→ 예상 관리 방법 및 견적
```

### 확장 후보

- 공간 유형: 사무실 / 카페 / 음식점 / 병원 / 학원 / 기타
- 가구 재질: 패브릭 / 가죽·인조가죽 / 목재 / 플라스틱 / 금속 / 모름
- 상태: 가벼운 먼지 / 얼룩·냄새 / 심한 오염 / 흔들림 / 찢김·파손 / 부품 누락
- 엘리베이터 유무, 작업 가능 시간, 주차 가능 여부
- 희망 작업일과 정기관리 주기

---

## 13. 최종 구현 원칙

- 사용자는 서비스 전문용어보다 **자신의 공간과 가구 상태**를 기준으로 답한다.
- 한 번에 많은 정보를 요구하지 않고 단계별로 나눠 입력받는다.
- 예상가는 확정 견적처럼 표현하지 않는다.
- 금액 계산이 어려운 경우에도 상담 신청으로 자연스럽게 이어지도록 한다.
- 첫 MVP의 성공 기준은 정교한 자동 견적이 아니라 **사용자가 1분 안에 조건을 입력하고 방문견적 신청까지 완료하는 것**이다.
