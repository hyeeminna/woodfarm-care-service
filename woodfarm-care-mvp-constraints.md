# WOODFARM CARE 간편 견적 MVP 제한사항 정의서

- 문서 버전: v1.0
- 작성일: 2026-09-16
- 목표 구현 시간: 1시간 이내
- 구현 권장 환경: React + TypeScript + Vite
- 적용 문서: `woodfarm-care-mvp-requirements.md`

---

## 1. 문서 목적

본 문서는 사업계획서 시연용 **WOODFARM CARE 간편 견적 MVP**를 제한된 시간 안에 안정적으로 구현하기 위해 개발 범위와 기술적 제약을 정의한다.

이 MVP의 목적은 실제 주문·결제·고객관리 시스템을 완성하는 것이 아니라, 다음 핵심 가설을 화면으로 검증하는 것이다.

> 사무실·카페·병원·식당 등에서 사용하는 가구를 교체하기 전에, 사용자가 관리 대상과 필요한 서비스를 선택하고 예상 비용을 확인한 뒤 방문견적을 신청할 수 있는가?

요구사항 정의서와 본 문서가 충돌할 경우, **1시간 구현에서는 본 제한사항 정의서를 우선 적용한다.**

---

## 2. 1시간 MVP의 최우선 구현 범위

아래 기능만 필수 구현한다.

1. 랜딩 화면과 `간편 견적 시작하기` 버튼
2. 4단계 견적 입력 흐름
   - STEP 1: 상주 인원 선택
   - STEP 2: 가구 종류와 수량 선택
   - STEP 3: 필요한 서비스 선택
   - STEP 4: 사진 선택 및 미리보기 또는 사진 없이 진행
3. 선택값을 이용한 예상 견적 결과 표시
4. 무료 방문견적 신청 폼
5. 필수 항목을 입력한 경우 신청 완료 화면 표시
6. 모바일·데스크톱에서 내용이 깨지지 않는 기본 반응형 UI

### 우선순위

```text
화면 흐름 완성
→ 입력값 저장과 검증
→ 견적 결과 표시
→ 신청 완료 화면
→ 반응형 및 디자인 보완
→ 사진 미리보기와 부가 기능
```

시간이 부족하면 하위 항목부터 축소하되, 랜딩부터 신청 완료까지의 전체 사용자 흐름은 유지한다.

---

## 3. 기능 제한사항

### 3.1 프론트엔드 데모로 한정

- 모든 화면과 계산은 브라우저의 로컬 상태에서 처리한다.
- 별도의 백엔드, 데이터베이스, 서버리스 함수, API를 구축하지 않는다.
- 새로고침하면 입력값과 신청 정보가 초기화되어도 된다.
- 여러 사용자 간 데이터 동기화 기능을 제공하지 않는다.

### 3.2 방문견적 신청

- 신청 버튼은 실제 신청 정보를 외부로 전송하지 않는다.
- 유효성 검사를 통과하면 신청 완료 화면만 표시한다.
- 제출 데이터는 필요 시 개발자 콘솔에 출력할 수 있으나 영구 저장하지 않는다.
- 이메일, 문자, 카카오톡, 네이버 톡톡, CRM 연동은 구현하지 않는다.
- 제출 실패를 재현하는 서버 오류 화면은 구현하지 않아도 된다.

### 3.3 사진 업로드

- 사진은 서버로 업로드하거나 저장하지 않는다.
- 브라우저 내 임시 미리보기 용도로만 사용한다.
- 최대 5장, 파일당 10MB 이하의 JPG, JPEG, PNG, WEBP만 허용한다.
- 카메라 직접 촬영, 드래그 앤 드롭, 이미지 편집·압축·회전 기능은 제외한다.
- AI 이미지 분석, 오염도 판정, 가구 종류 자동 인식 기능은 제외한다.
- 페이지를 벗어나거나 새로고침하면 사진이 사라져도 된다.
- 미리보기 URL을 생성했다면 사진 삭제 또는 컴포넌트 종료 시 해제하여 메모리 누수를 방지한다.

### 3.4 예상 견적 계산

- 온라인 결과는 확정 견적이 아닌 **참고용 예상 범위**로만 표시한다.
- 계산 가능한 기본 조합은 `사무용 의자`와 `파티션`의 `세척 + 상태점검`으로 한정한다.
- 초기 데모 단가는 다음 값만 사용한다.

| 가구 | 최소 단가 | 최대 단가 |
|---|---:|---:|
| 사무용 의자 | 7,000원/개 | 8,500원/개 |
| 파티션 | 4,000원/개 | 6,500원/개 |

- 소파 또는 책상이 포함된 경우 임의의 가격을 계산하지 않고 `방문 확인 필요`를 표시한다.
- `간단수리` 또는 `정기관리`가 포함되면 해당 비용을 임의로 더하지 않고 별도 상담 안내를 표시한다.
- 서비스별 할인, 최소 출장비, 거리별 출장비, 부가가치세, 재질, 오염도, 작업 난이도는 계산에 반영하지 않는다.
- 단가는 한 개의 설정 객체에서만 관리하며 화면 컴포넌트에 중복 작성하지 않는다.
- `의자 30개 + 파티션 10개 + 세척 + 상태점검`의 결과는 반드시 `약 25~32만 원`으로 표시한다.

### 3.5 입력 항목

- STEP 1~4에 정의된 항목 외의 추가 설문은 구현하지 않는다.
- 공간 유형은 견적 단계가 아니라 방문견적 신청 폼에서만 받는다.
- 오염·파손 정도, 가구 재질, 엘리베이터, 주차, 방문 희망일은 이번 버전에서 제외한다.
- 가구 수량은 정수 `1~999`만 허용한다.
- 연락처는 숫자와 하이픈만 허용하되, 실제 존재하는 번호인지 인증하지 않는다.
- 주소 검색 API를 사용하지 않고 방문 지역은 일반 텍스트로 입력받는다.

### 3.6 화면 이동 및 상태

- 별도 라우팅 라이브러리는 필수가 아니다.
- 하나의 페이지에서 현재 화면 상태를 변경하는 방식으로 구현할 수 있다.
- 브라우저 뒤로가기와 화면 단계의 `이전` 버튼을 완전히 연동하지 않아도 된다.
- STEP 간 이동 시 입력값은 유지한다.
- `새 견적 시작하기` 또는 `처음으로 돌아가기` 실행 시 모든 로컬 입력값을 초기화한다.

---

## 4. 기술 제한사항

### 4.1 기술 스택

- 기존 프로젝트의 React + TypeScript + Vite 환경을 유지한다.
- 상태 관리는 React의 `useState` 또는 `useReducer`만 사용한다.
- Redux, Zustand, MobX 등 별도 전역 상태 라이브러리를 추가하지 않는다.
- 기존 의존성으로 해결 가능한 경우 새 패키지를 설치하지 않는다.
- UI 프레임워크나 대형 컴포넌트 라이브러리를 새로 도입하지 않는다.
- 스타일은 기존 프로젝트 방식 또는 단순 CSS로 작성한다.

### 4.2 프로젝트 구조 변경

- 기존 `woodfarmgagu`의 현재 페이지와 기능을 삭제하거나 전면 개편하지 않는다.
- 청소·유지관리 MVP는 기존 사이트 안의 독립 섹션 또는 독립 페이지처럼 보이도록 구성한다.
- 기존 라우팅과 헤더·푸터가 있다면 재사용하되, 연결 작업 때문에 핵심 흐름 구현이 지연되면 MVP 화면만 우선 완성한다.
- 기존 파일명, 경로, 빌드 설정을 불필요하게 변경하지 않는다.
- 환경변수, 인증키, 외부 서비스 키를 요구하는 기능은 추가하지 않는다.

### 4.3 데이터와 개인정보

- 실명, 연락처, 사진 등 입력 데이터는 영구 저장하지 않는다.
- 실제 고객 정보를 테스트 데이터로 사용하지 않는다.
- 데모 제출 정보가 브라우저 저장소에 남지 않도록 `localStorage`와 `sessionStorage`를 사용하지 않는다.
- 사진 파일의 내용을 외부 서비스로 전송하지 않는다.
- 운영 서비스처럼 오해하지 않도록 신청 폼 또는 완료 화면에 `현재 화면은 MVP 데모이며 실제 접수 기능은 추후 연결 예정입니다.`라는 안내를 표시한다.

### 4.4 접근성과 호환성

- 최소 지원 화면 폭은 360px이다.
- 최신 Chrome과 Edge를 우선 지원한다.
- Safari의 세부 시각 차이까지 완벽하게 맞추는 작업은 후순위로 둔다.
- 키보드 포커스, 입력 라벨, 버튼 타입, 이미지 대체 텍스트 등 기본 접근성은 유지한다.
- 선택 여부를 색상만으로 표현하지 않고 체크 표시 또는 문구를 함께 사용한다.

### 4.5 성능

- 배경 동영상, 3D 효과, 대용량 이미지, 복잡한 애니메이션은 사용하지 않는다.
- 애니메이션은 CSS 기반의 짧은 전환 효과만 허용한다.
- 업로드 사진 원본을 변환하거나 분석하지 않는다.
- 이미지가 선택되지 않은 상태에서도 전체 플로우가 정상 동작해야 한다.

---

## 5. 디자인 제한사항

- 기존 WOODFARM 브랜드가 있다면 로고, 대표 색상, 기본 서체를 우선 재사용한다.
- 브랜드 정보가 없거나 적용에 시간이 걸리면 다음 기본 원칙을 사용한다.
  - 밝은 배경
  - 짙은 녹색 또는 우드 계열 포인트 컬러 1개
  - 흰색 카드
  - 둥근 모서리와 가벼운 그림자
- 페이지당 핵심 CTA는 하나가 가장 눈에 띄도록 한다.
- 과도한 그라데이션, 네온 효과, 3D 카드, 긴 등장 애니메이션은 사용하지 않는다.
- 무료 이미지 검색과 외부 이미지 다운로드는 필수가 아니다. 적합한 기존 이미지가 없으면 텍스트와 아이콘 중심으로 구현한다.
- 아이콘이 필요한 경우 이모지 또는 프로젝트에 이미 설치된 아이콘만 사용한다.
- 사업계획서 캡처 시 핵심 흐름이 잘 보이도록 각 화면의 정보량을 최소화한다.

---

## 6. 명시적 제외 범위

다음 기능은 이번 1시간 MVP에서 구현하지 않는다.

- 회원가입, 로그인, 사용자 권한
- 데이터베이스 및 관리자 페이지
- 실제 견적 신청 저장과 담당자 알림
- 온라인 결제, 예약, 계약, 전자서명
- 실제 AI 사진 분석 또는 추천
- 지도, 주소 검색, 방문 가능 지역 자동 판정
- 고객별 견적 이력과 견적서 PDF 생성
- 쿠폰, 할인, 포인트, 세금 계산
- 다국어 지원
- 정기관리 구독 및 자동 결제
- 실시간 상담 또는 챗봇
- 분석 도구, 광고 픽셀, 쿠키 동의 관리
- 검색엔진 최적화의 세부 설정
- 운영 수준의 보안·장애 대응·백업 체계

---

## 7. 시간 초과 시 축소 순서

구현 시간이 부족하면 아래 순서로 기능을 축소한다.

1. 로딩 화면의 애니메이션을 제거하고 결과를 즉시 표시한다.
2. 사진 미리보기를 단순 파일명 표시로 대체한다.
3. 사진 삭제 기능을 제외하고 파일 재선택 방식으로 대체한다.
4. `- / +` 스테퍼를 제거하고 숫자 입력창만 사용한다.
5. 화면 전환 애니메이션과 장식용 아이콘을 제거한다.
6. 문의사항 입력란과 보조 설명을 간소화한다.
7. 신청 데이터의 콘솔 출력을 제외한다.

다음 항목은 축소하지 않는다.

- STEP 1~3의 선택과 유효성 검사
- 가구 수량 입력
- 결과 화면의 선택 내용 요약
- `의자 30개 + 파티션 10개` 기준 견적 계산
- `방문 확인 필요` 처리
- 참고용 예상 견적 안내 문구
- 방문견적 신청 폼과 완료 화면

---

## 8. 완료 기준

아래 조건을 모두 충족하면 1시간 MVP 구현을 완료한 것으로 본다.

1. 개발 서버가 오류 없이 실행된다.
2. 랜딩 화면에서 신청 완료 화면까지 중단 없이 진행할 수 있다.
3. 필수 선택이나 입력이 없으면 다음 단계 또는 제출이 제한된다.
4. 이전 단계로 이동해도 기존 입력값이 유지된다.
5. 사진 없이도 견적 결과를 확인할 수 있다.
6. `의자 30개 + 파티션 10개 + 세척 + 상태점검` 결과가 `약 25~32만 원`으로 표시된다.
7. 소파 또는 책상을 선택하면 임의의 금액 대신 `방문 확인 필요`가 표시된다.
8. 결과 화면에 예상 금액이 확정 견적이 아니라는 안내가 표시된다.
9. 신청 폼 제출 후 완료 화면이 표시된다.
10. 360px 모바일 화면에서 가로 스크롤 없이 주요 기능을 사용할 수 있다.
11. 콘솔에 치명적인 런타임 오류가 없다.
12. 프로덕션 빌드 명령이 성공한다.

---

## 9. Kiro 구현 지시사항

Kiro는 다음 원칙에 따라 작업한다.

1. 먼저 기존 프로젝트 구조와 사용 중인 기술을 확인한다.
2. 기존 사이트의 동작과 스타일을 최대한 보존한다.
3. 본 제한사항의 최우선 범위부터 순서대로 구현한다.
4. 요구사항이 모호하면 새로운 기능을 추측해 추가하지 않고 가장 단순한 방식으로 구현한다.
5. 실제 백엔드나 외부 API가 필요한 기능은 모의 동작으로 처리한다.
6. 모든 견적 단가는 하나의 설정 객체에 분리한다.
7. 컴포넌트를 지나치게 세분화하지 않되, 화면·폼·견적 계산 로직은 구분한다.
8. 구현 후 핵심 경로와 기준 견적을 직접 확인한다.
9. 마지막에 빌드를 실행하고 오류가 있으면 신규 기능 추가보다 오류 수정에 우선한다.
10. 제한 시간 내 완료가 어려운 항목은 임의로 미완성 상태로 남기지 말고 `시간 초과 시 축소 순서`에 따라 제거하거나 단순화한다.

---

## 10. 최종 원칙

> 이번 MVP의 성공 기준은 완전한 청소·유지보수 플랫폼이 아니라, 기존 WOODFARM 가구 사업에 **가구 판매 이후의 세척·점검·간단수리·정기관리 서비스를 연결할 수 있다는 사업 흐름을 한눈에 보여주는 것**이다.

따라서 실제 연동 기능보다 사용자가 `조건 선택 → 예상 견적 확인 → 방문견적 신청`을 자연스럽게 경험하는 것을 우선한다.
